import { Bar } from 'react-chartjs-2';


const labels = [ 'Ihjas', 'Thenveer', 'Duyoof','Angel']
const data ={
    labels,
    datasets: [
        {
            label
        }
    ]
}

const Horizontalchart=()=>{
    return(<Bar data={} options={}/>)

}

export default Horizontalchart;
